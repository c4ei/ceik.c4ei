<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta property="og:title" content="CEIK WEB MINING(마이닝)">
    <meta property="og:url" content="https://ceik.c4ei.net/">
    <meta property="og:image" content="https://i.ibb.co/0Qc6Tcy/ceikmining-V6.jpg">
    <meta property="og:description" content="CEIK  무료 WEB MINING을 시작 하세요.">
    <meta name="description" content="CEIK - c4ei.net 4 klay" />
    <meta name="author" content="c4ei.net">
    <link rel="apple-touch-icon" href="/favicon.ico" />
    <link rel="shortcut icon" type="image/x-icon" href="/favicon.ico" />

    <title>Ladder List</title>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Noto Sans KR', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f0f0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        /* 간단한 스타일링 */
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid #ccc;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f4f4f4;
        }
        .pagination {
            margin-top: 20px;
            text-align: center;
        }
        .pagination a {
            margin: 0 5px;
            padding: 5px 10px;
            text-decoration: none;
            border: 1px solid #ccc;
        }
        .pagination a.active {
            background-color: #007bff;
            color: white;
            border: 1px solid #007bff;
        }
    </style>
</head>
<body>
    <a href="/">HOME</a> / <a href="/ladder"><%= __("text_80") %></a> <!-- 사다리 게임 -->
    <br>
    <h1><%= __("text_108") %> <!-- 참여한 게임 목록 --></h1>
    <table>
        <thead>
            <tr>
                <th><%= __("text_109") %></th><!-- 게임 ID -->
                <th><%= __("text_102") %></th><!-- 베팅 선택 -->
                <th><%= __("text_66") %></th><!-- 베팅 금액 -->
                <th><%= __("text_110") %></th><!-- 등록일 -->
                <th><%= __("text_71") %></th><!-- 결과 -->
                <th><%= __("text_111") %></th><!-- 승리 금액 -->
            </tr>
        </thead>
        <tbody>
            <% userBets.forEach(function(bet) { %>
                <tr>
                    <td><%= bet.game_id %></td>
                    <td><%= bet.bet_choice %></td>
                    <td><%= bet.bet_amount %></td>
                    <td><%= bet.YYMMDD %></td>
                    <td><%= bet.result %></td>
                    <td><%= bet.win_amount %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
    
    <!-- 페이징 UI -->
    <div class="pagination">
        <% if (currentPage > 1) { %>
            <a href="/ladderList?page=<%= currentPage - 1 %>"><%= __("text_112") %></a><!-- 이전 -->
        <% } %>

        <% for (let i = 1; i <= totalPages; i++) { %>
            <a href="/ladderList?page=<%= i %>" class="<%= currentPage === i ? 'active' : '' %>"><%= i %></a>
        <% } %>

        <% if (currentPage < totalPages) { %>
            <a href="/ladderList?page=<%= currentPage + 1 %>"><%= __("text_113") %></a><!-- 다음 -->
        <% } %>
    </div>
</body>
</html>
