<!-- join.ejs -->

<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta property="og:title" content="CEIK WEB MINING(마이닝)">
    <meta property="og:url" content="https://ceik.c4ei.net/">
    <meta property="og:image" content="https://i.ibb.co/0Qc6Tcy/ceikmining-V6.jpg">
    <meta property="og:description" content="CEIK  무료 WEB MINING을 시작 하세요.">
    <meta name="description" content="CEIK - c4ei.net 4 klay" />
    <meta name="author" content="c4ei.net">
    <link rel="apple-touch-icon" href="/favicon.ico" />
    <link rel="shortcut icon" type="image/x-icon" href="/favicon.ico" />
    <title><%= __("text_57") %></title><!-- 가입 -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Noto Sans KR', sans-serif;
            background-color: #f8f9fa;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .card {
            width: 400px;
        }
    </style>
</head>
<body>
    <div class="card">
        <div class="card-body">
            <h5 class="card-title text-center"><!-- 가입 --><%= __("text_57") %></h5>
            <form name="form_join" id="form_join" method="POST">
                <div class="form-group">
                    <label for="email">EMAIL:</label>
                    <input type="text" class="form-control" id="email" name="email" required  maxlength="50" >
                </div>
                <div class="form-group">
                    <label for="password"><!-- 비밀번호 --><%= __("text_59") %>:</label>
                    <input type="password" class="form-control" id="password" name="password" required  maxlength="50" >
                </div>
                <br />
                <div class="form-group">
                    <label for="fid"><!-- 추천인ID: --><%= __("text_60") %></label>
                    <input type="text" class="form-control" name="fid" value="<%= fid %>" readonly >
                </div>

                
                <input type="hidden" name="code" value="<%= code %>">
                <br />
                <button type="button" onclick="jsfn_submit();" class="btn btn-primary btn-block"><!-- 가입하기 --><%= __("text_282") %></button>
            </form>
            <% if(_resend=="Y"){ %> <script>alert('<%= __("text_61") %>');</script> <% } %><!-- 이미 가입된 email 입니다.\n현재 창을 닫고 다시 시도해 주시기 부탁드립니다. -->
            <script>
                function jsfn_submit(){
                    let _form_join = document.getElementById("form_join");
                    let _email = document.getElementById("email");
                    let _password = document.getElementById("password");
                    if(_email.value.length<10){
                        alert('<%= __("text_283") %>'); // email 을 입력하세요.
                        _email.focus();
                        return;
                    }
                    if(!validateEmail(_email.value)){
                        alert('<%= __("text_284") %>'); // email 형식을 확인 하세요
                        _email.focus();
                        return;
                    }
                    if(_password.value.length<8){
                        alert('<%= __("text_285") %>'); // 비밀번호를 8자이상 입력하세요.
                        _password.focus();
                        return;
                    }
                    _form_join.action="/joinok";
                    _form_join.submit();
                }
                function validateEmail(email) {
                    var re = /\S+@\S+\.\S+/;
                    return re.test(email);
                }
            </script>
        </div>
    </div>
</body>
</html>